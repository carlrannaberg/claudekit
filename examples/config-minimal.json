{
  // Minimal hook configuration
  // This example shows the bare minimum needed to get started
  "hooks": {
    // Just enable typecheck with defaults
    "typecheck": {},
    
    // Enable ESLint with defaults
    "eslint": {},
    
    // Enable auto-checkpoint with defaults
    "auto-checkpoint": {}
  }
}